#include <stdio.h>
#include <math.h>

int main() {
    int a = 3, b = 2;
    int sum = a + b;
    int multiply = a * b;
    int x, y = a * b;
    int power = pow(a, b);
    printf("%d", power);
    return 0;
}